# LazyVPS v1.2.11 IPv6 Only Clean

用于 IPv6 Stable 仍 Timeout 时，单独新建纯 IPv6 Trojan 测试端口。

## 菜单

```text
35) Stability Suite
7) IPv6 Mode
8) IPv6 Only Clean / 纯 IPv6 独立端口重作
```

## 快捷命令

```bash
bash /root/lazy-vps-menu.sh --quick ipv6-only
```

## 输出

```text
01_IMPORT_FLCLASH_IPV6_ONLY_PORT2443.yaml
```
