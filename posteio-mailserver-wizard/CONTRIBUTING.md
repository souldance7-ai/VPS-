# 贡献指南

欢迎提交 Issue 和 Pull Request。

提交前请完成：

```bash
bash -n posteio-wizard.sh
bash -n install.sh
shellcheck posteio-wizard.sh install.sh
```

贡献内容不得包含真实 IP、域名、邮箱、密码、令牌、私钥或用户日志。示例统一使用 RFC 5737 文档地址和 `example.com`。
