# LazyVPS TUIC / AnyTLS 模板说明

本目录仅放开源模板，不包含真实 IP、密码、UUID、证书私钥。
执行 `bash lazy-vps-menu.sh --quick anytls` 或 `bash lazy-vps-menu.sh --quick tuic` 后，真实导入文件会生成在：

```bash
/opt/lazy-vps-menu/outputs/
```

不要把 outputs 目录上传到公开仓库。
